#using the min() method for printing out the smallest element in the list
days = [1, 2, 3, 5, 9, 6, 7, 8]
min = min(days)
print(min)

#using the .len()
days = [1, 2, 3, 5, 9, 6, 7, 8, 9, 10]
length = len(days)
print(length)

#using the max() method for printing out the greatest element in the list
days = [1, 2, 3, 5, 9, 6, 7, 11 15]
max = max(days)
print(max)

#using sorted() method
days = [1, 2, 3, 4, 5, 6, 7, 11, 9, 8]
sort = sorted(days)
print(sort)

#using the .join method()
name = "--".join(["Kendrick", "Lamar"])
print(name)

#using the count() method
names = ['Jermaine', 'Rakim', 'Bobby', 'Royce', 'Marshall', 'Nas', 'Joey', 'Franklin']
name = names.count("Rakim")
print(name)

#using append() method
days = ['Saturday', 'Friday', 'Thursday', 'Wednesday', 'Tuesday', 'Monday', 'Sunday']
days.append('Days of the week')
print(days)

#using the clear() method
names = ['Jermaine', 'Rakim', 'Bobby', 'Royce', 'Marshall', 'Nas', 'Joey', 'Franklin']
names.clear()
print(names)


